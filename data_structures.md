# 과제 2 - 시나리오별 최적 자료구조 매핑 리포트

## 1. 상품코드를 입력하면 즉시 재고 수량을 확인해야 하는 재고 관리 데이터
### ① 선택한 자료구조
- **딕셔너리**
### ② 선택 근거
- **선택 이유**: 상품코드를 키, 재고 수량을 값으로 저장할 수 있다. 상품코드를 통해 그 값을 빠르게 조회할 수 있다.
- **다른 후보 배제 이유**:
    - **리스트**: 상품코드와 재고 수량을 서로 대응되는 형태로 저장하기 어렵다. 또한 특정 상품코드를 찾기 위해 데이터를 for 문을 이용해 순차적으로 확인해야 하므로, 데이터가 커질수록 느려지기 때문에 적합하지 않다.
    - **튜플**: 상품코드와 재고 수량을 서로 대응되는 형태로 저장하기 어렵다. 또한 리스트와 같이 특정 상품코드를 찾기 위해 데이터를 순차적으로 확인해야 하므로 적합하지 않다.
    - **셋**: 중복 제거와 소속 검사에 용이하나, 상품코드와 재고 수량을 서로 대응되는 형태로 저장하기 어렵기 때문에 적합하지 않다.
### ③ 예시 코드
```python
stock = {"A100": 5, "A200": 10, "A300": 15}
code = "A200"
print(f"상품코드 {code} 재고 수량: {stock[code]}")
```

## 2. 이벤트 응모 고객 명단에서 중복 응모를 제거하고, 기존 회원 명단과의 교집합을 구해야 하는 상황
### ① 선택한 자료구조
- **셋**
### ② 선택 근거
- **선택 이유**: 셋은 중복 값이 불가하여 자동으로 중복 제거를 할 수 있다. 또한 & 연산자를 통해 두 집합의 교집합을 쉽게 구할 수 있다.
- **다른 후보 배제 이유**:
    - **리스트**: 중복 제거와 교집합을 구하는데 별도의 코드 작성이 필요하기에 적합하지 않다.
    - **튜플**: 중복이 허용되고 & 연산자를 지원하지 않아 적합하지 않다.
    - **딕셔너리**: key-value 형태의 데이터에는 적합하나 현 상황에서는 적합하지 않다.
### ③ 예시 코드
```python
event_list = ["김서강", "박철수", "이알바", "이알바"] # 기존 이벤트 응모 고객 명단
event_clear = set(event_list) # 중복 제거
members = {"김서강", "이알바"} # 기존 회원 명단
members_event = event_clear & members # 기존 회원 명단과 교집합을 구한 명단
print(f"최종 명단: {members_event}")
```

## 3. 월별 매출액을 1월부터 12월까지 순서대로 저장하고 순회하는 데이터
### ① 선택한 자료구조
- **리스트**
### ② 선택 근거
- **선택 이유**: 데이터의 순서를 유지한다. 또한 인덱스를 통해 각 월의 매출액에 접근할 수 있다. 순서대로 저장하고 순회하는 상황에 적합하다.
- **다른 후보 배제 이유**:
    - **튜플**: 튜플은 데이터의 순서를 유지하고 순회할 수 있지만, 데이터를 수정할 수 없으므로 향후 월별 매출액을 수정해야 하는 경우 리스트보다 불편하다.
    - **딕셔너리**: 월-매출액 형태로 저장할 수 있으나, 단순히 데이터를 순서대로 저장하고 순회하는 목적으로는 리스트가 더 간단하다.
    - **셋**: 순서가 없고 중복 값이 불가함으로 적합하지 않다.
### ③ 예시 코드
```python
monthly_sales = [1200, 1350, 1100, 1500, 1600, 1750, 1900, 1850, 1650, 1700, 2000, 2300]
total_sales = 0
for sales in monthly_sales:
    total_sales += sales
print(f"총 매출: {total_sales}만원")
```

## 4. 한 번 발급되면 절대 변경되어서는 안 되는 (위도, 경도) 매장 좌표
### ① 선택한 자료구조
- **튜플**
### ② 선택 근거
- **선택 이유**: 튜플은 4개의 자료구조(리스트, 튜플, 딕셔너리, 셋) 중 값 변경이 불가한 자료구조로서 적합하다.
- **다른 후보 배제 이유**: 리스트, 딕셔너리, 셋 모두 값 변경을 허용하기 때문에, 절대 변경되어서는 안 되는 데이터의 자료구조로서 적합하지 않다.
### ③ 예시 코드
```python
location = (37.56, 126.91)
lat, lng = location
print(f"매장 위치: 위도 - {lat}, 경도 - {lng}")
```

## 5. 1,000만 줄짜리 웹 서버 접속 로그 파일에서 특정 조건의 줄 수를 세는 작업
### ① 선택한 자료구조
- **Iterator**를 활용한 파일 처리
### ② 선택 근거
- **선택 이유**: Iterator는 호출할 때마다 다음 값을 하나씩 내어주는 객체로, 1,000만 줄의 데이터를 메모리에 저장하지 않고 파일에서 필요한 데이터를 한 줄씩 처리할 수 있다. 현 상황에서는 조건에 맞는 줄의 개수만 세면 되므로 모든 로그를 메모리에 저장할 필요가 없어, 아주 적은 메모리만 사용하는 Iterator를 활용하는 것이 적합하다.
- **다른 후보 배제 이유**:
    - **리스트**: 1,000만 줄의 데이터를 모두 리스트에 저장하면 메모리 사용량이 매우 높아진다. 특정 조건의 줄 수를 세는 작업에서는 불필요하게 모든 데이터를 저장하는 것이므로 적합하지 않다.
    - **튜플**: 튜플 또한 전체 데이터를 메모리에 저장해야 하므로 적합하지 않다.
    - **딕셔너리**: 딕셔너리 또한 전체 데이터를 메모리에 저장해야 하므로 적합하지 않다.
    - **셋**: 각 줄을 순서대로 읽어 조건을 검사하는 작업에 적합하지 않다.
### ③ 예시 코드
```python
count = 0
with open(path, encoding="utf-8") as f:
    for line in f: #iterator 활용
        if "time: 13:30" in line:
            count += 1
print(f"13시 30분 접속 로그 줄 개수: {count}")
```